# Slack bugs 0028–0034

Implemented and verified locally on September 20, 2026. No commit, push, hosted migration, deployment, or Slack message was made.

| Report | Finding and change |
| --- | --- |
| [0028](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917514773279) | Seller history renders raw checkout attempts. Read-only V1 aggregates found six schedules with failed/expired attempts followed by paid checkouts and no repeated subscription IDs; the exact three-row report was not identifiable without its purchase ID. Group retries for the same customer, package, address snapshot, portions, dates, trial, total and renewal source. Show one sales row with expandable attempt statuses, timestamps and IDs. Deduplicate identical row IDs; separate successful financial events and different customers remain separate. No financial records are removed or rewritten. |
| [0029](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917516972719) | Seller tab, heading, row labels, empty state and unavailable-report copy use Penjualan / Sales. Customer and admin purchase terminology remains appropriate to those views. |
| [0030](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917518676219) | The renewal form explicitly rendered a second checkout link after preparing the address. It now navigates directly using a router transition; its action remains disabled during navigation. Address failures retain the form and its error handling. |
| [0031](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917520468719) | Localized, prominent delivery range and delivery-day count; explicit first/last delivery labels; individual dates and each cycle's boundaries remain visible in expandable sections. |
| [0032](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917524141909) | Start-date picker disables elapsed previous-day cutoffs in the caterer's timezone and non-operating weekdays. Keyboard navigation skips unavailable dates. A stale saved date cannot submit; eligibility refreshes while the screen remains open, and server cutoff validation remains authoritative. |
| [0033](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917526036369) | Read-only V1 query confirmed `multi_cycle=false` with nine packages configured for longer durations. Offers now expose the same rollout eligibility used by the quote function; customer selectors only show purchasable durations. Seller configuration and revision numbers remain intact, including when editing while rollout is off. Two- and three-cycle price/schedule/checkout flows pass when eligible. The hosted flag was not enabled. |
| [0034](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789917527943149) | Explicit terms acceptance controls the disabled payment button and a localized reminder. Re-reviewing or changing inputs resets acceptance. HTTP and direct database commands reject missing, false or non-boolean acceptance before writes. The authenticated checkout transaction records a server timestamp and terms version alongside reservations/outbox work. Acceptance is immutable and idempotent. Both payment-provider adapters reject creating a session without recorded acceptance. |

## Verification

- `npm run typecheck`: passed for web, customer workspace and root tests.
- `npm test`: 220 tests passed across 32 files. Existing purchase fixtures explicitly accept terms; the provider fixture carries its recorded acceptance. New negative cases verify rejection without checkout, reservation or outbox side effects.
- `npm run build`: passed after the final implementation changes.
- `npm run test:postgres`: 24 evidence checks passed, including concurrent consent/idempotency, capacity, payment confirmation, multi-cycle renewal, cross-tenant access and provider workers. Evidence: `output/slack-bugs/0028-0034/postgres.json`.
- 25 distinct browser scenarios passed across the focused run and reruns: four new two-cycle/consent/cutoff/sales scenarios, four three-cycle/renewal scenarios, one rollout control scenario and sixteen settlement scenarios. Final checkout rerun verifies today and tomorrow are disabled after cutoff, a later valid day remains enabled, re-review resets consent, payment creation records consent, and three attempts render as one expandable sales row.
- Indonesian/English, 390px/1440px checkout and sales screenshots inspected; existing settlement coverage also includes 360px/768px and 200% text. The existing multi-cycle scenarios include axe serious/critical checks. Final checkout checks report no document overflow or uncaught page errors.
- Sales browser regression injects a three-attempt response fixture around an actual local paid checkout. The grouping unit test independently checks duplicate IDs, multiple paid purchases and separate customers. Live aggregate diagnosis is separate from this synthetic reproduction.
- Isolated local demo: `CATERA_DEMO_DATA_DIR=.data/slack-0028-0034`, `CATERA_NEXT_DIST_DIR=.next-slack-0028-0034`, port 3034. No hosted customer data was copied into fixtures.

## Release requirements

Apply `supabase/migrations/20260920152746_checkout_sales_safeguards.sql` with the matching application release. It exposes duration eligibility and adds consent columns plus the transactional guard. Existing acceptance is left unknown; no historical consent is fabricated. New provider sessions for old pending checkouts without acceptance are rejected; those holds must expire and customers must review a new checkout. Existing completed purchases and provider confirmations remain unchanged.

Multi-cycle purchasing stays unavailable in a hosted environment where its rollout flag is off. Enabling it remains subject to the existing product/provider release gates. The migration and application have not been applied to V1 or the protected pilot. Hosted/provider and physical-device acceptance are not claimed.

The full unit suite initially exposed a missing consent field in an older provider fixture; that fixture was updated. Browser reruns updated stale selectors for the current duration dialog and discount label. All failures described here were resolved before the final passing runs.
