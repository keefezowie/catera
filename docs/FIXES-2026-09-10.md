# September 10 document fixes

Source: the user-supplied `fixes.docx`. Changes apply to branch `v1`.

## Implemented

- English navigation destinations: homepage discovery uses `/#packages`; subscription explanation uses `/#how-it-works`. Both scroll within the same page and preserve selected filters. The former `/discover` address redirects permanently and retains query parameters.
- Shared web dropdown: styled area, language, filter and form selectors use Radix Select, with keyboard navigation, focus restoration, scrolling, touch targets and viewport collision handling. Language choices show their full names; the header keeps compact ID/EN labels. Form submissions retain named values.
- Hosted authentication: email/password login complements the existing phone-code registration/login path. Sessions use the existing server-cookie integration. Database roles determine customer, seller and admin destinations; return paths reject external redirects. Invalid credentials and rate limits have localized messages. User metadata cannot assign authority.
- Four synthetic Supabase accounts: customer, owner, staff and platform administrator in V1 project `ygzfdqrljunngfrdygzt`. Owner and staff belong to a synthetic draft caterer. Provisioning created auth identities, profiles, memberships and an audit entry in one transaction. The hosted pilot project was not changed.

Credentials are in ignored `.data/demo-accounts.json`; do not commit or publish this file. `scripts/provision-demo-accounts.sql` documents the one-time provisioning operation without embedded passwords. `scripts/verify-demo-accounts.mjs` checks sign-in, database roles, admin denial for other roles, seller access and sign-out without printing credentials.

## Verification

- TypeScript checks, 48 unit/integration tests and the Next.js production build passed.
- PostgreSQL concurrency checks passed. Native component tests passed under both iOS and Android presets (four each).
- All six local browser scenarios passed, with the operational scenario rerun after making its service date follow an existing seeded delivery. Coverage includes purchase/payment, rescheduling, support, seller production CSV, operational routes, authorization failures, responsive layouts, anchors, language persistence and dropdown keyboard/touch behavior.
- No serious or critical Axe issues in the checked account page and open dropdown. Mobile dropdown screenshots: `output/fixes-verification/area-phone.png` and `language-phone.png`.
- Live Supabase sign-in, role permissions and sign-out passed for all four accounts; sanitized evidence is in `output/fixes-verification/supabase-auth.json`.

The hosted browser authentication suite is prepared in `tests/hosted/auth.spec.ts` and `playwright.hosted.config.ts`, but remains unverified: automatic approval review rejected starting the local server configured for hosted Supabase, without supplying a specific reason. These checks do not certify SMS/SMTP delivery, payment providers, production deployment or the other release gates in `RUNBOOK.md`.
