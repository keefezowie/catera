# Slack bug tracker

Channel: [#bugs](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS). Bug reports are reviewed when requested; no recurring monitoring is configured. Last full channel scan: September 11, 2026; newest message `1789143852.250369`.

## Latest fixed

September 20, 2026 — implemented ID0028–ID0034 locally: grouped seller payment attempts into expandable sales entries; renamed seller purchase history to Penjualan / Sales; direct renewal navigation with pending state; clearer delivery ranges and cycle boundaries; cutoff-aware start dates; duration selectors aligned with backend rollout eligibility; and required, server-recorded terms acceptance before checkout/payment creation. [Findings, per-report fixes, validation and migration requirements](SLACK-0028-0034.md). Verified: 220 unit tests, 24 PostgreSQL evidence checks, 25 distinct browser scenarios across focused runs, workspace typecheck and production build. Indonesian/English desktop/phone views inspected. Read-only V1 diagnosis confirmed multi-cycle rollout is off and retry rows exist; no hosted flags/data were changed. Local only; migration, deployment and Slack posting were not performed.

September 20, 2026 — moved package duration/discount editing into the shared modal at the user's request. Package cards now retain their height; the popup identifies the package, scrolls within the viewport, closes after saving, and restores focus to its trigger. Drafts remain available after closing/reopening; failed saves remain inside the popup. Eight ID/EN browser checks passed at 390/768/1440/1920px, including unchanged card height, save/reload, Escape and focus restoration. Typecheck, 215 unit tests and PostgreSQL concurrency checks passed. Local frontend change; no deployment or Slack post.

September 19, 2026 — DOKU sandbox live verification exposed payout polling SQL identifier collisions and an invalid nested data-modifying outbox CTE. Added guarded migrations, preserved service-role permissions and concurrent leases, and verified the hosted worker returns HTTP 200. Applied only to the dedicated DOKU sandbox database. A real sandbox BRI callback now activates one subscription automatically. Validation: 215 unit tests, typecheck, hosted build, and PostgreSQL worker/concurrency checks. [Integration and evidence](DOKU-SANDBOX-INTEGRATION.md). Existing Catera production was not changed; no Slack post.

September 19, 2026 — repaired the four user-supplied seller screenshots. Hosted V1 was missing settlement-reporting, seller-experience and beta-operations migrations; installed these existing migrations and verified attention/payout reads for every owner. Subscription/delivery/ledger/payout counts and ledger total were unchanged. Added a follow-up RPC permission migration to counter hosted default anonymous EXECUTE grants. Locally repaired the duration editor's placement in the photo column, overlapping fields, stretched neighboring cards, responsive totals, and per-meal seller prices. Validation: 198 unit tests, typecheck, production build, 21 PostgreSQL scenarios, and eight ID/EN browser regressions at 390/768/1440/1920px including save/reload. [Repair and hosted evidence](SELLER-SCREENSHOT-FIXES-2026-09-19.md). Database repair is live; frontend changes are local and not deployed. No Slack post.

September 19, 2026 — completed the user-requested desktop/mobile UX research and improvement pass. Added visible, persistent discovery filters and recovery; clearer package pricing and comparison; compact checkout summary, destination review, confirmed-total actions, and step focus; responsive local artwork; corrected shared semantics and caterer calendar filter retention. [Research, implementation, sources, and validation](UIUX-RESEARCH-2026-09-19.md). Verified locally: 132 route/width states with no reported axe violations, uncaught errors, or document overflow; 87 distinct browser scenarios across the main run and focused reruns; 198 unit tests; 21 PostgreSQL scenarios; typecheck and production build. Existing stale browser fixtures/selectors were updated to current workflows without dropping pending/error safeguards. This request was not a new Slack report. Production performance and physical-device acceptance remain unverified; automatic approval review blocked an additional production-mode test server. No hosted deployment or Slack posting.

September 16, 2026 — implemented ID0017–ID0027 locally: grouped Today orders and explorable metrics; disabled past seller menu dates; inline dish categories; validated direct-to-Storage upload flow; calendar-first customer detail and authoritative delivery-change feedback; seller-initiated conversations; reviewed payout destinations; focused Settings/account-help queue; and separate Caterer Profile. [Per-report behavior, source, security, evidence and release prerequisites](SLACK-0017-0027.md). Validation: typecheck, 184 unit tests, production build, 17 PostgreSQL concurrency/security scenarios and 57 targeted browser scenarios passed across the main run and focused reruns, with desktop/390px Indonesian/English inspection. Six stale broader browser scenarios remain unpassed (retired admin controls, preloaded dispute records and old Pilot results heading). Hosted acceptance for uploads, the tester's original delivery-change failure, and Xendit sandbox remains open. No hosted migration, deployment or Slack posting. Unrelated clean-catalog work is preserved.

September 12, 2026 — fixed [ID 0015](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789203091219689) and [ID 0016](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789203153161059). Unset dates now initialize empty category slots even when a legacy package template contains dishes; saved dated menus remain intact. The unsaved-menu dialog has 12px action gaps, outlined secondary buttons, stacked phone actions, and an explanation with disabled Save for incomplete menus. Inspected both Slack screenshots. Verified locally: legacy-template response fixture opens 0/4 slots, saved date opens 4/4, 1440px/390px dialog geometry and screenshots, keyboard navigation and retained draft, six menu browser scenarios, 103 unit tests, 11 PostgreSQL checks, workspace typecheck and production build. Browser checks used an isolated explicit synthetic demo on port 3015; only the legacy-template response was intercepted, with real dated-menu reads and the existing end-to-end suite using real local writes. Evidence: `output/slack-bugs/0015-0016/`. Local only; no deployment or Slack posting.

September 12, 2026 — completed the web UI sweep for IDs 0007, 0008, 0009, 0010 and 0012, plus the requested saved-date calendar treatment. Configured date cards now show dish names on a distinct background; fixed clipped library focus rings, the support-count badge, schedule spacing, composition stylesheet collisions and preview centering. The sweep also restored the 44px operational date control, preserved date/meal context between Today and Schedule, and corrected focus after saving menus. Verified: 59 distinct browser scenarios across the broad run and focused reruns, 103 unit tests, 31 native tests, 11 PostgreSQL checks, typechecks and production build. [Scope, screenshots and run details](UI-SWEEP-2026-09-12.md). Local only; no deployment or Slack posting.

September 12, 2026 - fixed [ID 0013](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789143620281079): published package terms and composition are immutable; drafts remain editable. Owners suspend packages to stop discovery/new purchases while serving existing customers, then archive only after deliveries and live payment holds finish. Late payments after archival enter support. Verified: 103 unit tests, 31 native tests, 11 PostgreSQL concurrency/security checks, typecheck/build, desktop and 390px browser interactions, persisted archive state and no horizontal overflow. [Rules and evidence](PACKAGE-LIFECYCLE.md); evidence in `output/slack-bugs/0013/`. Local only; hosted migration/deployment and Slack posting were not performed. Existing demo servers need a restart to load the migration.

September 12, 2026 — fixed [ID 0011](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789142861148999): nutrition facts now belong to the package editor instead of dated menus. Calories, protein, carbohydrates and fat each accept either one fixed value or a minimum–maximum range; package-level values render consistently in web/native discovery, details, comparison and immutable purchase snapshots. Existing menu-level values remain readable through a compatibility fallback and are not rewritten in historical purchases. Verified: 103 unit tests, 5 focused native tests, 7 relevant browser scenarios, PostgreSQL concurrency/security checks, typecheck, production build, desktop/390px interaction, and zero browser-console errors. Local implementation only; no Slack message, hosted migration or deployment. The Supabase migration list could not be checked because the local Supabase stack was not running on port 54322.

September 12, 2026 — fixed [ID 0010](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789142497750019): package composition counts now use locale-aware category names, including English labels such as “Count Main dish”. The add-category control exposes its expanded state and switches between plus/minus icons; its create form is now stacked and padded so it no longer overlaps the composition rows. Also completed a web and Expo customer localization sweep covering shared statuses/errors/menu summaries, dates, currency, metadata, CSV labels and package/gallery presentation. Validation: 103 unit tests, web/Expo/workspace typecheck, production build, and focused browser flow passed. Local implementation only; no Slack message, hosted migration or deployment.

September 11, 2026 — verified [ID 0014](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789143852250369) is already resolved and no longer reproducible: fresh loads of both `catera-eight.vercel.app` and the hosted V1 preview render Mleo Double Protein as “Menu not yet set · 2 Lauk · 1 Sayur · 1 Sup.” The local package detail also retains those category quantities with no dated dishes. The live catalog payload contains the same slot composition, so the reported “0 dishes” screenshot was a stale presentation rather than lost package data. No new source change, Slack message, promotion, or deployment was needed.

September 11, 2026 — fixed [ID 0005](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789135895917209): the seller workspace now exposes the shared ID/EN language selector in its operational topbar, persists the choice through the existing locale cookie, and localizes the seller sidebar, workspace chrome and topbar copy. Validation: seller locale browser regression passed at desktop and 390px phone width, including reload persistence and no horizontal overflow; 96 unit tests, web/Expo/workspace typecheck and production build passed. Local implementation only; no Slack message, hosted migration or deployment.

September 11, 2026 — fixed [ID 0006](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789135969621439): Hari ini now has lunch/dinner tabs, customer/address rows, status summaries and atomic bulk fulfillment updates. Jadwal is a date/package dashboard with production and full-day CSV revisions; old production/delivery URLs redirect. Verified: 96 unit tests, typecheck, production build, Android/iOS exports, ten PostgreSQL checks, and all ten targeted browser scenarios across the final run and focused reruns. The combined browser run initially hit a shared screenshot-file write collision and an incorrect toast-visibility assertion; isolated evidence paths and a class-state assertion resolved both. Desktop/mobile screenshots and axe checks passed. [Behavior, interfaces and reproduction](SELLER-OPERATIONS.md); evidence: `output/slack-bugs/0006/`. Local only; no hosted migration, deployment or Slack message.

September 11, 2026 — refined [ID 0003](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789132010623699) to the approved visual assembly: calendar-to-card expansion, full-slot selection, category-filtered drag/drop, automatic next slot, phone picker, and icon-led daily nutrition. Converted existing explicit local demo data to one package model. Validation: 96 unit tests, 17 related browser scenarios in one run, ten PostgreSQL checks, typecheck/build, Android/iOS exports, desktop/phone inspection and repeatable baseline restoration. [Details and evidence](SLOT-MENU-CALENDAR.md). No Slack message or hosted deployment.

September 11, 2026 — fixed [ID 0003](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789132010623699): category-count package templates for both types, categorized reusable dishes inside Menu, and a monthly calendar with single/multiple date editing, search, drag-and-drop and mobile selection. Batch writes are atomic; optimistic conflicts retain the draft. Existing listings convert on edit and purchased revisions remain immutable. Validation: 88 unit tests, web/Expo typecheck, production build, Android/iOS export, 15 related browser scenarios verified across the final targeted runs, eight PostgreSQL concurrency/security scenarios, and desktop/mobile visual inspection. Details: [SLOT-MENU-CALENDAR.md](SLOT-MENU-CALENDAR.md); evidence: `output/slack-bugs/0003/`. Local implementation only; no Slack message, hosted migration or deployment.

September 11, 2026 — fixed ID 0002: package capacity is now one shared recurring value across every selected operating weekday. The seller date-specific capacity/closure workflow and command caller were removed; existing date rows remain honored as legacy overrides so bookings and purchase snapshots are unchanged. Validation: shared-capacity browser flow, 87 unit tests, production build, PostgreSQL concurrency/security checks, and web/customer typechecks passed. The broader dishes/journey run remains 5/7 because two unrelated pre-existing journeys still time out.

September 11, 2026 — fixed ID 0001: seller-capable accounts now land on `/seller` from `/` by default, with a localized profile menu for session-scoped Caterer/Customer switching. Database roles and seller authorization remain unchanged. Validation: workspace browser flow 2/2, combined relevant browser checks 13/14 (the one failure is an unrelated navigation-control strict selector against a concurrent three-slide hero), typecheck, 80 unit tests, production build, PostgreSQL concurrency/security checks, and responsive screenshots.

September 10, 2026 — fixed the marketplace navigation and responsive discovery reports: the food hero now leads the catalog, location/search controls sit below it, the hero remains stable while filtering, language control spacing is tightened without shrinking its touch target, and desktop/mobile active navigation follows anchors, routes, reloads and Back. Validation: navigation regression 3/3, typecheck, unit tests, build and PostgreSQL concurrency checks passed. Committed and pushed as `5965246` to `v1`; two unrelated journey tests still fail.

## Current reports

| Report                                                                                                                                                                                            | Plan and status                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Hero and location layout](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789034247968729)                                                                                            | Implemented locally: hero leads, delivery/search controls begin the following catalog section. Interpret “next page” as the next section, preserving existing anchor navigation. Keep hero mounted while filtering to avoid scroll jumps. Screenshot attachment transport returned metadata but was not visually inspected; verify any subsequent clarification. |
| [Language spacing](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789033112399299)                                                                                                    | Implemented locally: specific selector overrides generic select spacing with 4px gap while preserving a 44px minimum touch target.                                                                                                                                                                                                                               |
| [Desktop current section](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789032923190529) and [follow-up](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789032929609669) | Implemented locally: derive selected section from scroll position, handle initial anchors after content appears, use aria-current, and handle the bottom of short pages.                                                                                                                                                                                         |
| [Phone navigation](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789032955566119)                                                                                                    | Read parent and reply `1789032998.956289`. Implemented locally: map routes and child pages to customer destinations so selected state persists after navigation, reload and Back. This report concerns responsive web; native source was not changed.                                                                                                            |
| ID 0001 — seller landing and workspace switch                                                                                                                                                     | Implemented: owner/staff accounts default to the seller workspace and redirect from `/` to `/seller`; the shared profile menu switches the browser session to customer mode and back. The mode cookie is not an authorization source; customer/public browsing and database role checks remain intact.                                                           |
| ID 0002 — shared recurring capacity                                                                                                                                                               | Implemented: one recurring capacity field now applies to all selected operating weekdays. The seller date-specific override UI is gone; legacy `v1.capacity` rows remain read-only compatibility data and still take precedence for availability.                                                                                                                |
| [ID 0005 — seller language selector and English copy](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789135895917209)                                                                 | Implemented locally: operational seller chrome now exposes the shared locale selector and responds to Indonesian/English changes, including reload persistence.                                                                                                                                                                                                  |
| [Unspecified spacing screenshot](https://gknight-workspace.slack.com/archives/C0C0XDLFPFS/p1789032164485409)                                                                                      | Needs screenshot inspection before closing. Existing commit `9bea733` fixes marketplace toolbar alignment, but the text alone does not establish that it resolves this report.                                                                                                                                                                                   |

## Validation and push gate

Base: `9bea733` on `v1`; fix commit `5965246` is pushed to `origin/v1`. Full browser suite is not passing.

- Typecheck, 48 unit tests and production build passed during this run; final build/typecheck repeated after anchor correction.
- Navigation regression suite: 3/3 passed with installed Google Chrome via `PLAYWRIGHT_CHROMIUM_EXECUTABLE`. Covers desktop anchor selection, direct anchor load, scroll back to top, 1440/390px hero/filter order, stable filtering, language gap/touch target, mobile route navigation, reload and Back.
- Desktop and phone screenshots visually inspected: `output/slack-bugs/catalog-1440.png` and `catalog-390.png`.
- React review: passive scroll listener, frame batching, cleanup of observer/listeners/frame, route-derived state, accessible current markers.
- PostgreSQL: all five concurrency/security checks passed with a fresh disposable local `catera_test` on port 15449; evidence in `output/verification/postgres.json`. Default embedded startup exited without new evidence; use an explicit disposable database and require fresh evidence, not exit code alone. Port 55449 was unavailable; 15449 worked.
- Full browser run: 11/15 passed initially. Both navigation failures were fixed and their 3-test suite then passed. Two broader journey failures remain unresolved: checkout quote returns “Akun ini tidak memiliki akses” (expected 10 preview days), and seller production has no expected “Unduh CSV” link. Their causes are not established. Investigate them before committing/pushing; do not weaken assertions or report all tests green.
- Automatic approval review rejected starting an isolated web test server with copied synthetic fixtures, reporting only “blocked by policy.” No isolated server was started; browser tests used the existing localhost:3000 server.
- No native implementation changed; workspace native typecheck passed. No physical-device certification or production deployment is claimed.

## Next run

Continue from `5965246` before starting duplicate work. Resolve the two journey failures, rerun the full required checks, then stage only intended source/tests/docs and selected evidence. Existing uncommitted artwork, APK, attachments and prior output changes belong to other work. Never force-push or include unrelated files. Record the next commit and verified remote hash here after a successful push. Notify in the originating task only for meaningful changes; Slack posting is not authorized.
