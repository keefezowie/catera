import React from 'react';import {render,fireEvent,waitFor,userEvent} from '@testing-library/react-native';
jest.mock('../src/context',()=>({useNative:()=>({actor:{id:'test'},ready:true,error:'',demo:false,locale:'id',offers:[],t:(id:string)=>id}),apiBase:'http://localhost:3000'}));
jest.mock('expo-router',()=>({router:{push:jest.fn()}}));
jest.mock('@react-native-community/datetimepicker',()=>({__esModule:true,default:()=>null}));
jest.mock('@expo/vector-icons',()=>({Ionicons:()=>null}));
import {Qty,Run,Btn,Txt} from '../src/ui';
test('portion controls retain one as minimum and the trial maximum',()=>{const update=jest.fn();const screen=render(<Qty value={1} max={2} onChange={update}/>);fireEvent.press(screen.getByLabelText('Kurangi porsi'));expect(update).toHaveBeenLastCalledWith(1);screen.rerender(<Qty value={2} max={2} onChange={update}/>);fireEvent.press(screen.getByLabelText('Tambah porsi'));expect(update).toHaveBeenLastCalledWith(2)});
test('failed capacity command explains the rejection without claiming success',async()=>{const action=jest.fn().mockRejectedValue(new Error('CAPACITY'));const screen=render(<Run label="Konfirmasi tanggal" action={action}/>);fireEvent.press(screen.getByText('Konfirmasi tanggal'));await waitFor(()=>expect(screen.getByText(/Porsi pada salah satu tanggal/)).toBeTruthy());expect(screen.queryByText('Perubahan tersimpan.')).toBeNull();expect(action).toHaveBeenCalledTimes(1)});
test('submitting a command disables repeated taps while the server is pending',async()=>{let finish!:()=>void;const action=jest.fn(()=>new Promise<void>(r=>finish=r));const screen=render(<Run label="Bayar" action={action}/>);fireEvent.press(screen.getByText('Bayar'));await waitFor(()=>expect(screen.getByRole('button').props.accessibilityState.disabled).toBe(true));fireEvent.press(screen.getByRole('button'));expect(action).toHaveBeenCalledTimes(1);finish();await waitFor(()=>expect(screen.getByText('Perubahan tersimpan.')).toBeTruthy())});
test('disabled coverage action cannot navigate and typography retains native scaling',async()=>{const fn=jest.fn();const screen=render(<><Btn disabled label="Di luar area" onPress={fn}/><Txt kind="title">Makanan berikutnya</Txt></>);await userEvent.setup().press(screen.getByRole('button'));expect(fn).not.toHaveBeenCalled();expect(screen.getByText('Makanan berikutnya').props.allowFontScaling).not.toBe(false)});
